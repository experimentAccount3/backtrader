language: python
python:
  - "2.7"


before_install:
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq



# command to install dependencies
install:
  - pip install six==1.9.0
  - pip install nose-exclude==0.2.0
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform



# command to run tests
script: 
  - nosetests -v -v --exclude-dir=backtrader/plot --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_backtrader_8ca967f9dd_0
  - python -m properform Push 47.88.50.110:15554 asdf_backtrader_8ca967f9dd_0 ./profile_backtrader_8ca967f9dd_0

 #2017-08-13 14:46:42 modified at 0